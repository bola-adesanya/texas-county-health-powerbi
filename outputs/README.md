# Outputs

Use this folder for generated extracts, such as:

```text
county-employment-info.xml
infectious-disease-frequency.json
```

Generated extracts are ignored by Git by default because they may contain data-derived outputs.
